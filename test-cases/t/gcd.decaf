class GreatestCommonDivisor
{

	void main()
	{
		int x, y, z;
		x = 10;
		y = 20;
		z = gcd(x, y);

		System.out.println(z);
	}

	// Funcion que calcula el maximo comun divisor
	int gcd(int a, int b)
	{
		if (b == 0) {
			return (a);
		} else {
			return (gcd(b, a % b));
		}
	}
}
